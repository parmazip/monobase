# Monobase Account App Configuration

# ============================================================================
# API Configuration
# ============================================================================
# Development: Used during local development (vite dev)
# Production: Fallback if runtime config (/config.json) is not available
VITE_API_BASE_URL=http://localhost:7213

# ============================================================================
# OneSignal Push Notifications
# ============================================================================
# Get your App ID from OneSignal dashboard: https://dashboard.onesignal.com
# This is safe to expose in the frontend (it's a public identifier)
# IMPORTANT: Use the SAME app ID across all apps (account, admin, etc.)
# Development: Used during local development (vite dev)
# Production: Fallback if runtime config (/config.json) is not available
VITE_ONESIGNAL_APP_ID=your-onesignal-app-id-here

# Optional: Tag this app instance for targeted notifications
# Only set if you want to filter notifications by app
# Leave commented for most use cases (users receive all notifications)
VITE_ONESIGNAL_APP_TAG=account

# ============================================================================
# Docker Deployment (Production)
# ============================================================================
# When running in Docker/Kubernetes, configuration is managed via runtime
# environment variables that generate /config.json dynamically:
#
# Docker ENV variables (set in docker-compose.yml or K8s deployment):
#   - API_URL: Backend API endpoint (e.g., https://api.example.com)
#   - ONESIGNAL_APP_ID: OneSignal app ID for push notifications
#
# The app fetches /config.json on startup and uses those values if available,
# falling back to the VITE_* build-time values above if runtime config fails.
#
# This allows zero-downtime config updates without rebuilding the Docker image.
