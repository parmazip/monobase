# Whitelist approach: Ignore everything except what's needed for Docker build

# Ignore everything
*

# Allow only what we need for the Docker build
!.next/standalone/
!.next/static/
!public/
!Dockerfile
!docker-entrypoint.sh
