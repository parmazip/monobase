# Monobase Website App Configuration

# ============================================================================
# API Configuration
# ============================================================================
# Backend API base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:7213

# ============================================================================
# Cross-App Navigation URLs
# ============================================================================
# Patient app URL (for signup links and navigation)
NEXT_PUBLIC_PATIENT_APP_URL=http://localhost:3001

# Provider app URL (for signup links and navigation)
NEXT_PUBLIC_PROVIDER_APP_URL=http://localhost:3002

# ============================================================================
# Signup URLs (optional - auto-generated from app URLs if not set)
# ============================================================================
# Direct signup URLs for patient and provider apps
# NEXT_PUBLIC_PATIENT_SIGNUP_URL=${NEXT_PUBLIC_PATIENT_APP_URL}/auth/sign-up
# NEXT_PUBLIC_PROVIDER_SIGNUP_URL=${NEXT_PUBLIC_PROVIDER_APP_URL}/auth/sign-up

# ============================================================================
# Docker Runtime Configuration
# ============================================================================
# When running in Docker, these environment variables can be set at runtime:
# - API_BASE_URL (maps to NEXT_PUBLIC_API_BASE_URL)
# - PATIENT_APP_URL (maps to NEXT_PUBLIC_PATIENT_APP_URL)
# - PROVIDER_APP_URL (maps to NEXT_PUBLIC_PROVIDER_APP_URL)
# - PATIENT_SIGNUP_URL (maps to NEXT_PUBLIC_PATIENT_SIGNUP_URL)
# - PROVIDER_SIGNUP_URL (maps to NEXT_PUBLIC_PROVIDER_SIGNUP_URL)
#
# Example:
# docker run -e API_BASE_URL=https://api.example.com \
#            -e PATIENT_APP_URL=https://patient.example.com \
#            -e PROVIDER_APP_URL=https://provider.example.com \
#            websiteapp:1.0.0
